#include "main.h"

/**
 * _abs  -  a function that computes the absolute
 * value of an integer
 * @n : the number who i try compute the absoulute value
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

